import RiseSlides from "../../src/rise-slides";

describe("Rise Slides - Unit", () => {
  it("example test", () => {
    expect(RiseSlides).toBeDefined();

    const component = new RiseSlides();
    expect(component).toBeDefined();
  });
});
